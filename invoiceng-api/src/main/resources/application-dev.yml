spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/invoiceng
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.invoiceng: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Development JWT secret (DO NOT use in production)
jwt:
  secret: dev-secret-key-for-local-development-only-change-in-production

# Paystack test keys
paystack:
  callback-url: http://localhost:3000/payment/callback

# Termii credentials for development
termii:
  api-key: TLIScaGQeixJkeqsLeLiENbXcmwanLBUQmWlsHSqZfqvUFgmUUNtOHaehGRvde
  sender-id: aydeggy
